<%= form_for @room, authenticity_token: true do |f| %>
  <%= f.label :topic %>:
  <%= f.text_area :topic, placeholder: 'Enter topic name' %>
  <%= f.submit 'send', class: 'btn btn-primary' %>
<% end %>
